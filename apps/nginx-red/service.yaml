---
apiVersion: v1
kind: Service
metadata:
  name: nginx-red
  namespace: nginx-red
  labels:
    app: nginx-red
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: nginx-red
